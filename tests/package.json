{
	"name": "tests",
	"version": "1.0.0",
	"dependencies": {
		"advanced-command-handler": "file:..",
		"dotenv": "^8.2.0"
	},
	"main": "bot.js",
	"type": "module",
	"scripts": {
		"start": "node .",
		"build:run": "cd .. && npm run build && cd tests && npm start"
	}
}
