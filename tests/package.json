{
	"name": "tests",
	"version": "1.0.0",
	"dependencies": {
		"advanced-command-handler": "file:..",
		"dotenv": "^16.4.4"
	},
	"main": "bot.js",
	"type": "module",
	"scripts": {
		"start": "node .",
		"build:run": "cd .. && npm run build && cd tests && npm start"
	}
}
